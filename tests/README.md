# Tests

Unit tests for the user-owned graphs (`graphs/core/` and `graphs/skills_agent/`).

The local development runtime is LangGraph's dev server. These tests focus on
the graph code in this repo rather than the server runtime.

## Running

```bash
uv run pytest
```
